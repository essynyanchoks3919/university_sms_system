CREATE TABLE Student (
    id INT AUTO_INCREMENT PRIMARY KEY,
    first_name VARCHAR(255),
    last_name VARCHAR(255),
    email VARCHAR(255)
);
-- Additional DDL scripts for other entities would go here
